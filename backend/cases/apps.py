"""
Django app configuration for cases app.
"""

from django.apps import AppConfig


class CasesConfig(AppConfig):
    """Configuration for the cases app."""
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'cases'
